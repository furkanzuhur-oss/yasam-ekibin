# Render.com ucretsiz dagitim plani (Blueprint).
# render.com -> New -> Blueprint -> bu repoyu sec.
services:
  - type: web
    name: yasam-ekibin
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: TAVILY_API_KEY
        sync: false
      - key: MONGODB_URI
        sync: false
      - key: MONGODB_DB
        value: yasam_ekibi
